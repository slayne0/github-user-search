const $imageProfil = document.querySelector(".image-profil");
$imageProfil.src = "./image/lune.png";

const response = await fetch("https://api.github.com/users/tdimnet", {
   const await = await.json
  });
